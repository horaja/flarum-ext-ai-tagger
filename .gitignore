# Composer - Crucial for PHP dependencies
/vendor/
composer.lock

# Flarum JavaScript compiled assets
# These are generated by your build process (e.g., with flarum-webpack-config)
/js/dist/
/dist/ # Some older extensions might use this top-level folder
/assets/ # Flarum might place compiled frontend assets here under some circumstances

# IDE / Editor specific - Good for team collaboration
.vscode/

# OS specific - Good for cross-platform collaboration
.DS_Store
Thumbs.db

# PHPUnit cache (if you add tests later)
.phpunit.result.cache

# Node.js dependencies (if you use npm/yarn for JS build tools locally within the extension)
# You usually commit package.json and package-lock.json, but not the downloaded modules.
/node_modules/

# Log files or other temporary local files
*.log
temp/
tmp/